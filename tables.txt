lockerSlot | CREATE TABLE `lockerslot` (
  `id` int NOT NULL AUTO_INCREMENT,
  `status` tinyint(1) DEFAULT NULL,
  `tupcID` varchar(20) DEFAULT NULL,
  `initial` tinyint(1) DEFAULT NULL,
  `dateTime` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `tupcID` (`tupcID`)

users | CREATE TABLE `users` (
  `id` int NOT NULL AUTO_INCREMENT,
  `tupcID` varchar(20) NOT NULL,
  `balance` decimal(10,2) NOT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `tupcID` (`tupcID`)

 totalLocker | CREATE TABLE `totallocker` (
  `total` int DEFAULT NULL

currentCharge | CREATE TABLE `currentcharge` (
  `fee` decimal(10,2) DEFAULT NULL

 lockerHistory | CREATE TABLE `lockerhistory` (
  `id` int NOT NULL AUTO_INCREMENT,
  `tupcID` varchar(50) NOT NULL,
  `slotNumber` int NOT NULL,
  `action` varchar(20) NOT NULL,
  `dateTime` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`)